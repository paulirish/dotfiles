# Easier navigation: .., ..., ~ and -
alias cd..='cd ..'
alias ..="cd .."
alias cd..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~" # `cd` is probably faster to type though
alias -- -="cd -"

# be nice
alias please=sudo
alias hosts='sudo $EDITOR /etc/hosts'   # yes I occasionally 127.0.0.1 twitter.com ;)

# handy things
alias bunyip='node ~/code/bunyip/cli.js'
alias spotify="osascript ~/code/SpotifyControl/SpotifyControl.scpt"

# Detect which `ls` flavor is in use
if ls --color > /dev/null 2>&1; then # GNU `ls`
  colorflag="--color"
else # OS X `ls`
  colorflag="-G"
fi

# List all files colorized in long format
alias l="ls -l ${colorflag}"

# List all files colorized in long format, including dot files
alias lsa="ls -la ${colorflag}"

# List only directories
alias lsd='ls -l | grep "^d"'

# Always use color output for `ls`
if [[ "$OSTYPE" =~ ^darwin ]]; then
  alias ls="command ls -G"
else
  alias ls="command ls --color"
  export LS_COLORS='no=00:fi=00:di=01;34:ln=01;36:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arj=01;31:*.taz=01;31:*.lzh=01;31:*.zip=01;31:*.z=01;31:*.Z=01;31:*.gz=01;31:*.bz2=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.jpg=01;35:*.jpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.avi=01;35:*.fli=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.ogg=01;35:*.mp3=01;35:*.wav=01;35:'
fi

# `cat` with beautiful colors. requires Pygments installed.
# sudo easy_install Pygments
alias c='pygmentize -O style=monokai -f console256 -g'

# GIT STUFF
alias g="git"

# Undo a `git push`
alias undopush="git push -f origin HEAD^:master"

# git root
alias gr='[ ! -z `git rev-parse --show-cdup` ] && cd `git rev-parse --show-cdup || pwd`'

alias gs="git status"
alias gc="git commit -m "
alias gp="git pull"
alias gpp="git pp"
alias gfp="git fp"
alias gf="git fetch -p"
alias gfp="git fp"

alias ts="tig status"

# IP addresses
alias ip="dig +short myip.opendns.com @resolver1.opendns.com"
alias localip="ipconfig getifaddr en1"
alias ips="ifconfig -a | perl -nle'/(\d+\.\d+\.\d+\.\d+)/ && print $1'"

# Enhanced WHOIS lookups
alias whois="whois -h whois-servers.net"

# Flush Directory Service cache
alias flush="dscacheutil -flushcache"

# View HTTP traffic
alias sniff="sudo ngrep -d 'en1' -t '^(GET|POST) ' 'tcp and port 80'"
alias httpdump="sudo tcpdump -i en1 -n -s 0 -w - | grep -a -o -E \"Host\: .*|GET \/.*\""

# Trim new lines and copy to clipboard
alias trimcopy="tr -d '\n' | pbcopy"

# Recursively delete `.DS_Store` files
alias cleanup="find . -name '*.DS_Store' -type f -ls -delete"

# File size
alias fs="stat -f \"%z bytes\""

# ROT13-encode text. Works for decoding, too! ;)
alias rot13='tr a-zA-Z n-za-mN-ZA-M'

# Empty the Trash on all mounted volumes and the main HDD
alias emptytrash="sudo rm -rfv /Volumes/*/.Trashes; rm -rfv ~/.Trash"

# Hide/show all desktop icons (useful when presenting)
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"

# PlistBuddy alias, because sometimes `defaults` just doesn’t cut it
alias plistbuddy="/usr/libexec/PlistBuddy"

# One of @janmoesen’s ProTip™s
for method in GET HEAD POST PUT DELETE TRACE OPTIONS; do
  alias "$method"="lwp-request -m '$method'"
done

# Stuff I never really use but cannot delete either because of http://xkcd.com/530/
alias stfu="osascript -e 'set volume output muted true'"
alias pumpitup="osascript -e 'set volume 10'"
alias hax="growlnotify -a 'Activity Monitor' 'System error' -m 'WTF R U DOIN'"

# Some other Shortcuts
alias v="vim"
alias e="vim -c 'startinsert'"
alias edit='vim'
alias cls='clear'
alias ccat='pygmentize -O style=monokai -f console256 -g'
alias edpr='subl ~/.bash_profile'
alias rd='rm -rf' # Remove directories without asking me
alias rm='rm -f' # Remove files without asking me

# goto my folders
alias wiki='cd ~/Projects/wiki'
alias dropbox='cd ~/DropBox'
alias dev='cd ~/DropBox/Develop'
alias blog='cd ~/Projects/WebSites/blog'
alias mh='cd ~/Projects/WebSites/myhomepage'
alias pr='cd ~/Projects'
alias swk='cd ~/Projects/github/swk'
alias gtgs='cd ~/Projects/NodeProjects/gitsync'
alias mdw='cd ~/Projects/NodeProjects/mdwiki'
alias logfiles='cd ~/Projects/logfiles'
alias dotfiles='cd ~/Projects/dotfiles'
alias dockerfiles='cd ~/Projects/dockerfiles'
alias sublime-settings='cd ~/DropBox/_AppSettings/SublimeText'
alias dupl='cd ~/Projects/NodeProjects/duplicatesongfinder/'
alias howcani='cd ~/Projects/howcani'
alias howcani-api='cd ~/Projects/go/src/github.com/howcani-project/howcani-api'
alias mykatas='cd ~/Projects/Katas/MyKatas'
alias wiki='cd ~/Projects/WebSites/wiki-content'

if [ $UID -ne 0 ]; then
  alias reboot='sudo reboot'
  alias update='sudo apt-get upgrade'
fi

if [ $(uname) = "Darwin" ]; then
  alias swift="/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift"
fi

# aliases for my own functions
alias runTests=watchTests

# Run tmux with a predefined set
alias devmux='tmux -f ~/.dev.tmux.conf attach'
alias tmuxexit='tmux kill-server'
alias exitall=tmuxexit

alias reload="source ~/.zshrc"

# some aliases for go development
alias got='go test -v ./... | sed ''/PASS/s//$(printf "\033[32mPASS\033[0m")/'' | sed ''/FAIL/s//$(printf "\033[31mFAIL\033[0m")/'''

# some js aliases
alias lint="eslint ./"

# ranger
alias r="ranger"

# mount and unmount JABASOFT-DS
alias mount-nas="~/bin/mount-drives.sh"
alias unmount-nas="~/bin/unmount-drives.sh"

# mount MyDocuments
alias mount-mydocuments="~/bin/mount-mydocuments.sh"
alias unmount-mydocuments="~/bin/unmount-mydocuments.sh"

if [ $(uname) = "Linux" ]; then
  # start the VPN to my compan
  alias vpn="~/bin/vpn.sh"

  # Backup homefolders
  alias rsync-jabasoft-linux1="~/bin/rsync-JABASOFT-LINUX1.sh"

  # You need to install xsel with apt-get before using it
  alias pbcopy='xsel --clipboard --input'
  alias pbpaste='xsel --clipboard --output'
fi
