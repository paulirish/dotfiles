{
  "name": "@paulirish/agents",
  "version": "0.1.0",
  "description": "A monorepo for Paul Irish's Gemini skills and CLI agents.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.11.0"
  },
  "scripts": {
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "preflight": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
