#! /bin/bash
if [ -z $1 ]
then
  echo "you did not send a branch name"
  exit
fi
# go to master
git checkout master
# reset history 5 commits
git reset HEAD~5 --hard
# pull master history
git pull origin master
# create new branch
git checkout -b $1
# create new remote
git push origin $1
