import React from 'react';
import propTypes from 'prop-types';

const Foo = props => {
  return (
    <div className={'row'}>
      {props.children}
    </div>
  );
};

Foo.propTypes = {
  children: propTypes.any,
};

export default Foo;

